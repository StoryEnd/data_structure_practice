#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    char ch, s[20];
    char sen[100];
    scanf("%c", &ch);
    scanf("%s", s);
    fflush(stdin);
    gets(sen);
    printf("%c\n", ch);
    printf("%c\n", s);
    printf("%s\n", sen);


    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
//    scanf("%")
    return 0;
}
